开始转换：1.给start线一个脉冲；2.用spi写命令。
完成单次转换后，芯片自动进入休眠模式。

上电顺序：
1.DVDD上拉；
2.RESET下跳脉冲；若RESET连高，则发复位命令；上电后，等待时间100ms，参考figure45.
若用命令复位芯片，先用CS先复位SPI口，再用命令复位芯片。复位后保持0.6ms。

若用命令复位芯片，接着要用PO INIT命令初始化设备，若用RESET线，则不用。

RDATAC命令要在DRDY变低后发出，在下次DRDY低时有效。

内部电压参考需要操作IDAC